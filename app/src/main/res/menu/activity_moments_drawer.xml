<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/category" android:icon="@android:drawable/ic_menu_camera"
            android:title="Category" />
        <item android:id="@+id/testimonials" android:icon="@android:drawable/ic_menu_gallery"
            android:title="Testimonials" />
        <item android:id="@+id/report" android:icon="@android:drawable/ic_menu_slideshow"
            android:title="Report" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item android:id="@+id/nav_share" android:icon="@android:drawable/ic_menu_share"
                android:title="Share" />
            <item android:id="@+id/nav_send" android:icon="@android:drawable/ic_menu_send"
                android:title="Send" />
        </menu>
    </item>

</menu>